name: Build and Deploy Backend and Frontend

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
      - 'frontend/**'
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ secrets.GITHUB_ACTOR }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Build and Push Backend Docker Image
        run: |
          docker buildx build \
            --platform linux/arm64,linux/amd64 \
            -t ghcr.io/${{ github.repository }}/receipt-scanner:latest \
            ./backend --push

      # - name: Set up Node.js
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: '18'

      # - name: Install Frontend Dependencies and Build
      #   working-directory: frontend/receipt-scanner
      #   run: |
      #     npm install
      #     npm run build

      # - name: Upload Frontend Build to MinIO Bucket
      #   env:
      #     MINIO_ENDPOINT:    ${{ secrets.MINIO_ENDPOINT }}
      #     MINIO_ACCESS_KEY:  ${{ secrets.MINIO_ACCESS_KEY }}
      #     MINIO_SECRET_KEY:  ${{ secrets.MINIO_SECRET_KEY }}
      #     MINIO_BUCKET_NAME: ${{ secrets.MINIO_BUCKET_NAME }}
      #   run: |
      #     mc alias set myminio https://$MINIO_ENDPOINT $MINIO_ACCESS_KEY $MINIO_SECRET_KEY
      #     mc cp -r build/* myminio/$MINIO_BUCKET_NAME
